IF (KOKKOS_ENABLE_HPX)
  FIND_PACKAGE(HPX REQUIRED)
  MESSAGE(STATUS "KOKKOS_ENABLE_HPX: ${HPX_DIR}")
ENDIF()

IF (KOKKOS_ENABLE_HWLOC)
  IF (KOKKOS_HWLOC_DIR)
    FIND_PACKAGE(HWLOC REQUIRED HINTS ${KOKKOS_HWLOC_DIR})
  ELSE()
    FIND_PACKAGE(HWLOC REQUIRED)
  ENDIF()
  MESSAGE(STATUS "KOKKOS_ENABLE_HWLOC: ${HWLOC_DIR}")
ENDIF()

IF (KOKKOS_ENABLE_MEMKIND)
  IF (KOKKOS_MEMKIND_DIR)
    FIND_PACKAGE(MEMKIND REQUIRED HINTS ${KOKKOS_ENABLE_MEMKIND})
  ELSE()
    FIND_PACKAGE(MEMKIND REQUIRED)
  ENDIF()
  MESSAGE(STATUS "KOKKOS_ENABLE_MEMKIND: ${MEMKIND_DIR}")
ENDIF()

